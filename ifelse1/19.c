//Write a program to calculate the volume of a cuboid.(Input the side lengths of the cuboid from the user)
#include<stdio.h>
int main()
{
int l,w,h,c;
scanf("%d %d %d",&l,&w,&h);
c=l*w*h;
printf("%d",c);
return 0;
}
